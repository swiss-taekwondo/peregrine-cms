{
  "name": "materialize",
  "version": "1.0.0",
  "description": "customized materialize build for peregrine cms admin console",
  "repository": {
    "type": "git",
    "url": "https://github.com/headwirecom/peregrine-cms.git",
    "directory": "admin-base/materialize"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass -q peregrine.scss target/classes/etc/felibs/materialize/materialize.css",
    "patch": "node ./uploadCSS.js http://localhost:8080 admin:admin",
    "build:patch": "npm run build && npm run patch"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "fs-extra": "^7.0.1",
    "sass": "^1.89.2"
  }
}
